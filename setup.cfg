[tool:pytest]
testpaths = tests
addopts =
    -v
    --tb=short
    --cov=src
    --cov-report=term-missing
    --cov-report=html:htmlcov

[flake8]
max-line-length = 100
exclude =
    .venv,
    __pycache__,
    build,
    dist,
    .eggs,
    notebooks
ignore =
    E203,
    W503
